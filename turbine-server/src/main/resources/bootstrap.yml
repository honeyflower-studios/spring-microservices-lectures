spring:
  application:
    name: turbine-server
  cloud:
    config:
      uri: http://localhost:9999
server:
  port: 7987 #tell to use this port, in case server will not find remote configuration     